<%= render :partial => 'search' %>
<% @categories.each do |category| %>
  <div class="category">
    <%= link_to image_tag('category.jpg', :size => '100x100'), products_path(:category_id => category.id) %>
    <%= link_to category.name, products_path(:category_id => category.id) %>
  </div>
  
<% end %>
